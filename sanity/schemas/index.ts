import { portfolioItem } from './portfolioItem'

// Register all schemas here. Add new document types to this array
// (e.g. siteSettings, teamMember, testimonial) as the project grows.
export const schemas = [portfolioItem]
